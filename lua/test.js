
import { fun } from './util.js' 

// Function
function test() {

  const abc = 5
    if (abc > 10) {
    let list = []
    list.forEach((item,lakjfgh拉开街坊邻居阿里斯顿开房记录拉萨快点解封啦adskljf, index) => {
      item.isA && clearTimeout(5)
    })
  }

  let count;

  let ccc = 5 + 3 * 100

  if (ccc > 10) {
    count = 100
  } else {
    count = 200
  }

  console.log(count)

  fun()

  const nnnn = 555
  if (Math.random() > 0.5) {
    console.log(1024)
  } else {
    console.log(555)
  }

  function (a, b, c) {
    return String(a)
    // Math.random.bind
  }
}

class f {

}
